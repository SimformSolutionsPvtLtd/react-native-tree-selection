const Strings = {
  samePropsError:
    'Please check your props. titleField and childField props must be deferent.',
  textFieldTypeIssue: 'titleField should be string value only',
};

export default Strings;
