const Strings = {
  samePropsError:
    'Please check your props. titleKey and childKey props must be deferent.',
  textFieldTypeIssue: 'titleKey should be string value only',
};

export default Strings;
